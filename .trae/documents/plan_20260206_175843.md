Я подготовлю структуру сайта, разделив его на две версии:
1.  **`/love`** — Сохраняем текущую версию сайта (Love Story + профессиональные навыки) без изменений.
2.  **`/` (Главная)** — Создаем новую профессиональную версию, убирая упоминания Кирилла, но сохраняя яркий и продающий стиль ("voice tone").

### План действий:

1.  **Создание страницы `/love`**:
    *   Создам файл `src/pages/Love.tsx` как точную копию текущего `Home.tsx`.
    *   Добавлю маршрут `/love` в `src/App.tsx`.

2.  **Адаптация компонентов для Главной**:
    *   Создам `src/components/HeroProfessional.tsx` (на основе `Hero`):
        *   Уберу "(по версии Кирилла)".
        *   Оставлю акцент на "Топ-1 Директолог" и "бетонных заводах".
    *   Создам `src/components/AboutProfessional.tsx` (на основе `About`):
        *   Заменю пункт "Супер Девушка (Кирилл подтвердит)" на профессиональное качество (например, "Перфекционист" или "Нацелена на ROI").
        *   Остальные пункты ("2 Диплома", "Яндекс.Директ", "Авитолог") оставлю — они продающие.

3.  **Обновление Главной страницы (`Home.tsx`)**:
    *   Заменю `Hero` на `HeroProfessional`.
    *   Заменю `About` на `AboutProfessional`.
    *   **Уберу** компонент `LoveStory`.
    *   Оставлю кейсы, резюме и дизайн-кейс — они работают на продажу услуг.

4.  **Проверка**:
    *   Убежусь, что по `/love` открывается старая версия с Кириллом.
    *   Убежусь, что на главной `/` — строгая, но харизматичная версия специалиста.
